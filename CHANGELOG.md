# Changelog

## v0.2.15 (2026-09-10)

- 修复机器人图片附件只有名称和图标、无法打开的问题：根据图片标识读取受保护媒体，直接显示缩略图，点击进入全屏预览。
- 全屏支持双指缩放、拖动、双击放大／还原及关闭；网络失败可重试，明确显示登录失效、无权限和图片不存在。
- 图片下载与解码在后台进行，限制并发、下载大小和解码尺寸，使用有容量上限的内存缓存；切换账号清空缓存，私有图片不写入磁盘缓存。
- 支持已有会话图片及发送后保存的图片附件；保留 0.2.14 的滚动稳定性、长会话优化和表格分页。

## v0.2.14 (2026-09-10)

- 修复流式输出时反复启动滚动动画导致的抖动：底部按排版后的新增高度跟随，上翻和展开详情时保持阅读位置，返回底部后恢复跟随。
- 长回复按消息块懒加载，避免一轮任务同时创建全部思考和工具卡片；缓存历史对账结果，避免每次输入或输出都扫描全部历史正文。
- 历史 JSON 解码、Markdown 解析、工具详情格式化移至后台；纯文本流式更新合并显示，最终状态和审批等控制变化及时处理。
- 超长正文、思考和工具详情分页查看，保留完整内容；大表格每页显示 30 行，保留表头和独立横向滑动。

## v0.2.13 (2026-09-09)

- 修复回复中 Markdown 表格被当作普通文本显示的问题：增加表头、行列边框与交替行底色。
- 列宽按内容分配，长文本自动换行；宽表格可独立横向滑动，生成新行时保留滚动位置。
- 保留表格中的加粗、行内代码、链接、对齐与换行，支持引用和列表内的表格；代码块中的表格示例仍显示为代码。

## v0.2.12 (2026-09-09)

- 会话列表、导航侧栏和聊天顶部可删除会话，先确认目标，再调用服务器 DELETE 接口。
- 删除成功后退出被删除的当前会话，清理其草稿、附件与待处理记录。失败保留内容并显示错误。
- 删除过程中切换到其他会话不会被旧请求关闭；防止重复删除请求及旧列表响应让已删除会话重新出现。
- 使用原有签名，可覆盖安装并保留登录状态。MCP 调用与 max 思考档位由服务端提供。

## v0.2.11 (2026-09-08)

- 补充内容拿到模型消费回执后显示为主对话中的用户消息，自动撤掉输入框上方的队列提示。
- 服务端回执携带插入位置；多条同批补充按提交顺序显示，断线恢复不重复插入。
- 历史接口保留任务 ID，完成后使用该任务的完整用户／助手轮次，修复插入内容被过滤及旧轮次重复显示。
- 补充记录仅在历史确认包含相应文字后自动清理；失败与状态不明的内容仍可取回。
- 配套线上配置：14 个模型通过合成图片实测后补上 vision 能力。安卓系统文件选择到 WebSocket 的图片字节与 MIME 保持完整。

修复 `session_runtime.history_inconsistent`：聊天历史中的 NUL 空字符改为可见标记，避免 PostgreSQL 22P05 错误导致整轮回复保存失败；字面的反斜杠转义文本和 ACP 原始检查点保持原样。


## v0.2.10 (2026-09-06)

- 支持运行中连续提交多条文字补充；输入框继续保留有内容提交、空白停止的操作。
- 新服务端缓存补充，在运行时下一次可接收时按顺序合并当前缓存为一批；每条内容保留独立回执。
- 队列加密保存，兼容 v0.2.9 单条缓存；前台重连与后台监听核对整组状态，不自动重发。
- 每个会话最多保存 128 条本地补充记录；每轮服务端最多保留 128 条回执，最多 32 条待处理文字，待处理合计 32000 UTF-16 字符。
- 接收结果不明时保留待核对状态，明确拒绝后续尚未发送的缓存，避免重复插入或永久排队。


## 0.2.9 (2026-09-06)

- 回到前台时重新建立只读订阅并核对最新快照，恢复漏收的任务、工具及补充消息状态。
- 后台任务监听在完成通知前加密保存补充回执；前台和后台共享可观察记录，重新打开会话仍保留已确认结果。
- 已插入或已拒绝不会被旧排队快照、迟到控制回执或缺失回执的任务状态覆盖。
- 断线及前台恢复期间，未确认的补充显示待核对；同步只发送订阅，不重发补充内容或启动新任务。

## 0.2.8 (2026-09-06)

- 执行期间输入框有内容显示提交箭头，点击补充到当前任务；输入为空显示停止按钮。排队中禁止重复补充。
- 兼容服务端重启后从数据库恢复的 `messages: null`，修复旧会话一直“正在连接服务器”、新会话正常的问题。
- 仅有任务状态、没有正文的恢复快照不再隐藏最后一条历史回复。
- 解析异常、状态不一致和订阅无响应会明确报错；异常增量重新读取完整快照，避免静默卡住或重复订阅。
- 任务已结束而补充回执缺失时改为待核对，保留正文，不再一直显示排队，也不自动重发。

## 0.2.7 (2026-09-06)

- 新增运行中补充文字：先加密暂存，在执行端下一次处理消息的间隙插入；排队与实际插入分开确认，断线不重放，失败可恢复正文。
- 原生运行时、配套 Pi ACP 和 Codex ACP 支持补充；旧服务端禁用入口，其他 ACP 明确拒绝并保留内容。
- 配套服务端 bridge 取消健康连接的 30 分钟强制回收，修复长任务的 `ACP agent failed to complete the turn`；任务不依赖手机持续连接。
- 增加后台断开后继续发布结果、长连接、补充回执/重连/加密存储及输入区回归验证。

## 0.2.6 — versionCode 10 · 工具详情单项展开

- 工具详情的展开状态由会话页面统一管理，历史消息和实时回复共用一个展开项；打开其他工具自动折叠前一个，再次点击当前工具可收起。
- 使用所属轮次和工具调用标识区分工具，避免不同消息内相同编号互相干扰；切换机器人或会话时重新从折叠状态开始。

## 0.2.5 — versionCode 9 · 批准与交互回答

- 待批准／待回答面板移到输入区，显示当前请求并按队列处理；兼容省略操作能力字段，保留显式不可操作与已结束请求的只读状态。
- 补齐单选、多选、文本、自定义回答、可选问题跳过和取消请求，按服务端问题结构校验并发送 `user_input_response`。
- 批准展示具体命令／请求与 Agent 选项，支持拒绝原因；只有明确的允许选项才发送 approve，始终保留拒绝入口。
- 控制回执核对会话、运行任务和类型；已确认结果防止被旧 pending 状态覆盖，旧序号快照不回滚已结束任务，提前到达的终态释放按钮，正常的“请求已结束”回执不再误报错误。
- 匹配控制请求的错误不再误判为聊天连接断开；失败可重试，重连不自动重发，完成或不可操作的请求不再触发待决定通知。

## 0.2.4 — versionCode 8 · 记住会话模型

- 模型和思考强度从内存映射改为本机偏好，选中时提交保存，重新打开应用和已有会话时恢复；退出登录后原账号的选择仍保留。
- 保存按服务器、账号、机器人、会话和运行时隔离；仅存模型 ID／强度，不保存消息或凭据，不修改机器人全局默认。
- ACP 会话恢复已确认的模型与强度；需要调整运行时设置时逐项 PATCH 并等待确认，恢复失败可重试，确认前禁止发送。
- 仍可选择“跟随默认模型”；模型不可用时提示并回退，接口临时失败不会清除已保存选择。


## 0.2.3 — versionCode 7 · 通知左侧图标

- 移除等待、结果和锁屏公开版本通知的 large icon，取消右侧重复的 Memoh logo。
- 左侧身份图标沿用系统标准模板；应用图标改用新的 Memoh 资源引用，补齐五档密度的彩色 PNG 兼容资源，系统通过自适应图标处理圆形等遮罩。
- 单色 small icon 同样改用新的官方 Memoh 资源引用，设置品牌紫色；保留系统对状态栏与通知图标的着色规则。
- 不更改通知渠道、点击返回会话、提醒权限或内容隐私策略。


## 0.2.2 — versionCode 6 · 模型选择浮层与思考强度

- 按参考图改为模型按钮上方的紧凑浮层：顶部搜索、供应商分组、名称单行优先、右侧勾选，无全屏遮罩与“完成”按钮。
- 模型列表和输入区按钮隐藏 UUID，保留内部标识用于请求和区分同名模型。
- 新增真实思考强度选择，按钮显示“模型名 · 强度”；宽屏侧边子菜单，窄屏在浮层内展开，返回可回到模型列表。
- 原生模型使用 reasoning 能力、直接 Agent 使用各模型 reasoning_efforts，ACP 使用运行时 available_efforts。只提供服务端支持的档位，切换模型后重新校正默认值。
- 原生／直接 Agent 发送 reasoning_effort；ACP 使用官方 reasoning PATCH 接口，以服务器确认状态为准，失败时保留原值并提供重试。
- 名称不依赖供应商读取权限，供应商列表读取失败时仍可选择模型；搜索无结果有明确提示，浮层适应系统栏与键盘可见区域。


## 0.2.1 — versionCode 5

- 聊天输入区增加系统文件选择器，支持多选、附件卡片、移除、读取失败重试和仅发送附件。按官方协议以 data URL 随 WebSocket 消息提交，单条最多 10 个文件、合计 8 MB。
- 增加设备选择，读取官方工作区列表并向发送请求传递 `workspace_target_id`；离线设备不可选，绑定工作目录／Agent 会话按官方规则由绑定决定设备。
- 增加模型搜索与选择：普通模型目录、Codex／Claude Code Agent 模型目录、ACP 运行时模型目录。ACP 模型切换需服务器确认，失败可刷新恢复；普通选择以 `model_id` 随消息发送。
- 附件和模型／设备选择按账户及会话隔离；附件仅保留在内存，发送未排队时保留，排队成功只移除本次提交的文件，登出清除。
- 等待回复、完成、错误、审批和监听暂停的系统通知补上官方紫色 Memoh 大图标；状态栏继续使用官方 logo 的单色轮廓。
- 锁屏通用通知同样带品牌图标；原有通用文案、隐私可见性和点击返回会话的行为保持一致。
- 沿用发布包名和签名，可直接覆盖升级 0.2.0。

## 0.2.0 — versionCode 4

- 按 Memoh 官方 mobile shell 重写客户端：手机导航抽屉、768 dp 起常驻侧栏、机器人切换、会话搜索、居中阅读区和双行输入胶囊。
- 采用官方 SVG logo，覆盖应用内品牌、启动器自适应图标和单色通知图标；依据官方设计 tokens 对齐浅色与深色配色。
- 登录页采用官方居中 logo、登录文案、简洁表单及点阵背景；增加服务器 URL 和“记住登录信息”。
- 成功登录且用户勾选后，URL、用户名、密码一起用独立 AndroidKeyStore AES/GCM 密钥加密保存。取消勾选立即清除，退出登录保留已记住表单，错误密码不覆盖已有记录。
- 修复正常回复结束后出现“发生错误”：运行时空 error 字符串曾被非空值判断误当成错误，再套用默认报错。现在过滤空白值，同时为真实 errored/lost 保留失败提示。
- 思考和工具详情可折叠；流式 Markdown、复制回复、回到最新消息；翻阅历史时不抢滚动位置。
- 修复在聊天中刷新会话列表会离开聊天的问题；从导航切换机器人时释放旧聊天连接，保留账户／会话草稿隔离。
- 通过原有和新增单元／设备回归，验证记录与截图随交付包附带。当前包仍是原生客户端，未把 Web 全套管理功能打包为 WebView。

## 0.1.2 — versionCode 3

修复发布，沿用 `icu.minq.memoh` 和既有发布证书，可从 0.1.1 覆盖升级。

- R1：认证会话代次与条件提交，防止登出后旧刷新复活或清除新账户。HTTP 调用支持协程取消；503、网络错误和取消不再清空凭据。
- R2：统一可观察 pending 状态、完整账户/会话/调用身份隔离。遗留记录在启动时只读核对；结果未知有明确操作入口，不再无提示永久禁发。
- R3：返回/取消/登出显式释放 loading，陈旧加载结果不能重新覆盖页面。
- R4：回应服务器 WebSocket Close，及时禁用发送，串行安排唯一重连；snapshot 缺口恢复期间禁止发送。
- R5：低高度平板登录降级滚动单栏；正常宽屏表单也可滚动，IME 弹出时不切换布局丢失焦点。创建会话表单可滚动。
- R6：按账户/会话保存内存草稿，旋转不丢失。仅成功排队后清除匹配草稿，失败和迟到回调不会吞掉新文本。
- R7：完成、错误、审批、监听暂停的系统通知均只使用通用文案；原始服务端错误不进入通知。
- R8：终态历史请求绑定 session/epoch/seq/run/status，保留终态投影，不覆盖后续运行态。
- R9：禁止断线重放消息；接纳、离线及监听有界超时。超时表示 UNKNOWN 而非执行失败。服务按操作/startId/authEpoch 隔离，旧启动不误停新监听；不同审批 ID 分别通知。

### 验证与限制

- 自动回归包括认证竞态、实际 TLS WebSocket 关闭重连/不重放、草稿、pending 期限/身份和通知文案。
- Android 设备测试使用隔离存储和模拟 API，覆盖加载返回、pending 可观察状态、遗留记录、错误通知隐私、陈旧服务启动和监听期限。
- 设备实测范围与正式 Release 覆盖升级/真实链路证据见交付包 `VALIDATION.md`；不能将单元测试或 Debug 验证等同于所有系统/真机通过。
- 仍不支持交互式 user-input 表单提交、附件、历史分页和多并发 pending；草稿不跨进程持久化。
- 前台服务最长 30 分钟，不是离线推送或永久守护。厂商省电策略/系统强制停止可能中断监听。

## 0.1.1 — versionCode 2

手机/平板宽度断点、网格、登录双栏与聊天限宽。

## 0.1.0 — versionCode 1

原生 Kotlin/Compose 首版；HTTPS 登录、Memoh 会话及 Remote ACP、流式聊天、停止/审批和回复通知。
